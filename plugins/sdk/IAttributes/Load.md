# IAttributes.Load method (1 of 8)

将 DataRow 中的数据导入到实例中。 如果现有数据和导入数据名称冲突，系统将使用导入的数据覆盖现有的数据。

```csharp
public void Load(DataRow row)
```

| parameter | description |
| --- | --- |
| row | 需要导入的数据。 |

## See Also

* interface [IAttributes](../IAttributes.md)
* namespace [SiteServer.Plugin](../../SiteServer.Plugin.md)

---

# IAttributes.Load method (2 of 8)

将 DataRowView 中的数据导入到实例中。 如果现有数据和导入数据名称冲突，系统将使用导入的数据覆盖现有的数据。

```csharp
public void Load(DataRowView rowView)
```

| parameter | description |
| --- | --- |
| rowView | 需要导入的数据。 |

## See Also

* interface [IAttributes](../IAttributes.md)
* namespace [SiteServer.Plugin](../../SiteServer.Plugin.md)

---

# IAttributes.Load method (3 of 8)

将 Dictionary 中的数据导入到实例中。 如果现有数据和导入数据名称冲突，系统将使用导入的数据覆盖现有的数据。

```csharp
public void Load(Dictionary<string, object> dict)
```

| parameter | description |
| --- | --- |
| dict | 需要导入的数据。 |

## See Also

* interface [IAttributes](../IAttributes.md)
* namespace [SiteServer.Plugin](../../SiteServer.Plugin.md)

---

# IAttributes.Load method (4 of 8)

将 IDataReader 中的数据导入到实例中。 如果现有数据和导入数据名称冲突，系统将使用导入的数据覆盖现有的数据。

```csharp
public void Load(IDataReader reader)
```

| parameter | description |
| --- | --- |
| reader | 需要导入的数据。 |

## See Also

* interface [IAttributes](../IAttributes.md)
* namespace [SiteServer.Plugin](../../SiteServer.Plugin.md)

---

# IAttributes.Load method (5 of 8)

将 IDataRecord 中的数据导入到实例中。 如果现有数据和导入数据名称冲突，系统将使用导入的数据覆盖现有的数据。

```csharp
public void Load(IDataRecord record)
```

| parameter | description |
| --- | --- |
| record | 需要导入的数据。 |

## See Also

* interface [IAttributes](../IAttributes.md)
* namespace [SiteServer.Plugin](../../SiteServer.Plugin.md)

---

# IAttributes.Load method (6 of 8)

将 NameValueCollection 中的数据导入到实例中。 如果现有数据和导入数据名称冲突，系统将使用导入的数据覆盖现有的数据。

```csharp
public void Load(NameValueCollection attributes)
```

| parameter | description |
| --- | --- |
| attributes | 需要导入的数据。 |

## See Also

* interface [IAttributes](../IAttributes.md)
* namespace [SiteServer.Plugin](../../SiteServer.Plugin.md)

---

# IAttributes.Load method (7 of 8)

将匿名对象中的数据导入到实例中。 如果现有数据和导入数据名称冲突，系统将使用导入的数据覆盖现有的数据。

```csharp
public void Load(object anonymous)
```

| parameter | description |
| --- | --- |
| anonymous | 匿名对象。 |

## See Also

* interface [IAttributes](../IAttributes.md)
* namespace [SiteServer.Plugin](../../SiteServer.Plugin.md)

---

# IAttributes.Load method (8 of 8)

将JSON字符串转换为键值对后导入到实例中。 如果现有数据和导入数据名称冲突，系统将使用导入的数据覆盖现有的数据。

```csharp
public void Load(string json)
```

| parameter | description |
| --- | --- |
| json | 需要导入的数据。 |

## See Also

* interface [IAttributes](../IAttributes.md)
* namespace [SiteServer.Plugin](../../SiteServer.Plugin.md)

<!-- DO NOT EDIT: generated by xmldocmd for SiteServer.Plugin.dll -->
